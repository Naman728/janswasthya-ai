# Janswasthya AI — copy to .env (never commit real keys)

# Your OpenEnv / FastAPI backend for POST /step
OPENENV_BASE_URL=http://127.0.0.1:8000

# Hackathon LiteLLM / OpenAI-compatible proxy (injected in CI; do not hardcode keys)
API_BASE_URL=
API_KEY=

MODEL_NAME=gpt-4o-mini
